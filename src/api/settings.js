
// access to server on desktop
// The private IP allows me to connect from other computers or devices on the same network
// and This is particularly helpful for mobile testing. Responsiveness
export const SERVER_HOST = '192.168.12.189:8015'; //'localhost:8015'
export const BASE_URL = `http://${SERVER_HOST}/api/v1`; 